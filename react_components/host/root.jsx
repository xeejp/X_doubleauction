module.exports = (function(){
  var default_data = {
  };

  return React.createClass({
    getDefaultProps: function() {
      return default_data;
    },
    render: function () {
      return (
        <div>
        </div>
      );
    }
  });
})();
